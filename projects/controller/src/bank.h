#pragma once

typedef struct pti_palette_t pti_palette_t;
typedef struct pti_bitmap_t pti_bitmap_t;

extern pti_palette_t *palette;

// font
extern pti_bitmap_t *bitmap_a;
extern pti_bitmap_t *bitmap_b;
extern pti_bitmap_t *bitmap_x;
extern pti_bitmap_t *bitmap_y;

extern pti_bitmap_t *bitmap_left;
extern pti_bitmap_t *bitmap_right;
extern pti_bitmap_t *bitmap_up;
extern pti_bitmap_t *bitmap_down;

extern pti_bitmap_t *bitmap_start;
extern pti_bitmap_t *bitmap_select;
extern pti_bitmap_t *bitmap_rshoulder;
extern pti_bitmap_t *bitmap_lshoulder;